{
  "name": "github-actions-self-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Self-tests for the reusable workflows in this repo. NOT published or consumed by downstream repos.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
