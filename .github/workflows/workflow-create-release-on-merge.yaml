name: Create Release on Merge to Main

on:
  push:
    branches: ["main"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: ./.github/workflows/create-release-on-merge.yaml
    permissions:
      contents: write
      pull-requests: read
